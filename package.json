{
  "private": true,
  "scripts": {
    "build": "spago build",
    "serve": "parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
